import SwiftUI

struct ContentView: View {
    var body: some View {
        LocalWebView()
            .ignoresSafeArea()
            .background(Color(red: 7 / 255, green: 17 / 255, blue: 28 / 255))
    }
}
